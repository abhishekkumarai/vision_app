The goal is to create a mediapipe application that tracks whatever is in front of it and uses llm to identify it and provide information about it.

Uses:
- MediaPipe: 
- LLM: gemma 3.2b or any other
- UI: html/css/js for the frontend, tailwind for styling
- Python as the backend - fastapi for api 

Always Use UV for python package and its virtual environment.